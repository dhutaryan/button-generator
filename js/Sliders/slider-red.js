'use strict'

import SliderComponent from './slider-component.js';

export default class SliderRed extends SliderComponent {
    constructor(element, eventName) {
        super(element, eventName);
    }
}